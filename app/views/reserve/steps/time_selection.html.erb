<%= render 'shared/card',
  title: 'ご希望の時間を選択してください',
  subtitle: "#{@selected_date.strftime('%Y年%m月%d日')}（#{%w[日 月 火 水 木 金 土][@selected_date.wday]}）の予約可能時間" do %>

  <div class="space-y-6">
    <!-- 選択した日付の表示 -->
    <div class="bg-blue-50 border-l-4 border-blue-500 p-4 rounded">
      <p class="text-lg font-semibold text-blue-900">
        選択した日付: <%= @selected_date.strftime('%Y年%m月%d日') %>
        （<%= %w[日曜日 月曜日 火曜日 水曜日 木曜日 金曜日 土曜日][@selected_date.wday] %>）
      </p>
    </div>

    <!-- 時間枠選択グリッド -->
    <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
      <% @available_slots.each do |slot| %>
        <%= link_to reserve_steps_customer_path(slot_id: slot.id),
              class: 'group relative p-6 bg-white border-2 border-gray-300 rounded-lg hover:border-green-500 hover:shadow-lg transition-all transform hover:scale-105' do %>

          <!-- 時間表示 -->
          <div class="text-center">
            <p class="text-2xl font-bold text-gray-800 group-hover:text-green-600">
              <%= slot.starts_at.strftime('%H:%M') %>
              <span class="text-gray-500">〜</span>
              <%= slot.ends_at.strftime('%H:%M') %>
            </p>

            <!-- 空き状況 -->
            <p class="mt-2 text-sm text-gray-600">
              残り <span class="text-lg font-semibold text-green-600"><%= slot.remaining_capacity %></span> 枠
            </p>
          </div>

          <!-- 選択アイコン -->
          <div class="absolute top-2 right-2 opacity-0 group-hover:opacity-100 transition-opacity">
            <svg class="w-6 h-6 text-green-500" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd"/>
            </svg>
          </div>
        <% end %>
      <% end %>
    </div>

    <% if @available_slots.empty? %>
      <div class="bg-yellow-50 border-l-4 border-yellow-500 p-4 rounded">
        <p class="text-yellow-800">
          この日は予約可能な時間がありません。別の日をお選びください。
        </p>
      </div>
    <% end %>

    <!-- 戻るボタン -->
    <div class="flex justify-between mt-6">
      <%= link_to 'カレンダーに戻る',
          reserve_steps_calendar_path(
            branch_id: @branch.id,
            year: @selected_date.year,
            month: @selected_date.month
          ),
          class: 'btn btn-secondary' %>
    </div>
  </div>
<% end %>
