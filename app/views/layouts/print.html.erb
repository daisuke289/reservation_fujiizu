<!DOCTYPE html>
<html lang="ja">
  <head>
    <title><%= content_for(:title) || "印刷 - 富士伊豆農業協同組合 予約システム" %></title>
    <meta charset="utf-8">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    
    <style>
      @media print {
        body {
          font-size: 12pt;
          line-height: 1.4;
          color: black;
        }
        
        .no-print {
          display: none !important;
        }
        
        .page-break {
          page-break-before: always;
        }
        
        table {
          border-collapse: collapse;
          width: 100%;
        }
        
        th, td {
          border: 1px solid #000;
          padding: 4px 6px;
          text-align: left;
        }
        
        th {
          background-color: #f0f0f0;
          font-weight: bold;
        }
        
        .header {
          margin-bottom: 20px;
        }
        
        .branch-section {
          margin-bottom: 30px;
        }
        
        .branch-title {
          font-size: 14pt;
          font-weight: bold;
          margin-bottom: 10px;
          border-bottom: 2px solid #333;
          padding-bottom: 5px;
        }
      }
      
      @media screen {
        body {
          background: white;
          padding: 20px;
          max-width: 210mm;
          margin: 0 auto;
        }
        
        .print-button {
          position: fixed;
          top: 20px;
          right: 20px;
          z-index: 1000;
        }
      }
    </style>
  </head>

  <body>
    <div class="print-button no-print">
      <button onclick="window.print()" class="bg-blue-600 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded">
        印刷
      </button>
      <a href="<%= admin_dashboard_path %>" class="ml-2 bg-gray-600 hover:bg-gray-700 text-white font-bold py-2 px-4 rounded">
        戻る
      </a>
    </div>

    <%= yield %>
  </body>
</html>